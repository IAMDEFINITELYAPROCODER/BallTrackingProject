package Filters;

import Interfaces.PixelFilter;
import core.DImage;

public class BallTrackingProject implements PixelFilter {

    @Override
    public DImage processImage(DImage image) {
        return image;
    }
}
